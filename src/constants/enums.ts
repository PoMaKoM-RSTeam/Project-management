export enum Colors {
  RED = '#e92c64',
  GREEN = '#009886',
  BLUE = '#208eed',
  VIOLET = '#912f84',
  YELLOW = '#b36619',
  PINK = '#6e1d96',
}

export enum PathUrl {
  HOME = '',
  LIST = 'list',
  BOARD = 'board',
  SEARCH = 'search',
}

export enum PathTitle {
  LIST = 'List',
  BOARD = 'Board',
}

export enum LinkIcon {
  HOME = 'home',
  LIST = 'unordered-list',
  BOARD = 'project',
  SEARCH = 'eye',
  ABOUT = 'question',
  SETTINGS = 'more',
}

export enum Width {
  MEDIUM = '400px',
}

export enum Board {
  TITLE = 'To Do',
  TEXT = 'Example card item',
  COMMENT = 'Some comment',
}
