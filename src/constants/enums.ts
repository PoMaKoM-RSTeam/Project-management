export enum Colors {
  RED = '#e92c64',
  GREEN = '#009886',
  BLUE = '#208eed',
  VIOLET = '#912f84',
  YELLOW = '#b36619',
  PINK = '#6e1d96',
}

export enum PathUrl {
  HOME = '',
  BOARD = 'board',
}

export enum PathTitle {
  LIST = 'List',
  BOARD = 'Board',
}

export enum LinkIcon {
  LIST = 'unordered-list',
  BOARD = 'fund',
}

export enum Width {
  MEDIUM = '400px',
}

export enum Board {
  TITLE = 'To Do',
  TEXT = 'Example card item',
  COMMENT = 'Some comment',
}

export enum Message {
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
}
